# Data

Place local IEMOCAP audio and metadata here. The raw dataset is not included in git.

Recommended metadata file:

```csv
audio_id,audio_path,true_label
Ses01F_impro01_F000,data/iemocap/path/to/file.wav,neutral
```

The notebooks keep dataset paths in variables near the top so they can be changed easily.

